// Sandbox module for the generated Go SDK. Copied into gen/go by
// scripts/sandbox-build.sh, which then runs `go mod tidy` and `go build`.
// Nothing here is published; it exists to prove the schema generates code
// that compiles.
module github.com/the-protobuf-project/protobuf-rfc/gen/go

go 1.23
