# Membership Service

**Package:** `protobuf.rfc4519.membership.v1`

> [!IMPORTANT]
> Auto-generated by `protodoc`. Do not edit manually.

## Services

### Memberships

Memberships manages presence in a Group, RFC 4519 section 2.17 <https://www.rfc-editor.org/rfc/rfc4519.html#section-2.17>.  Deletion is permanent: a membership is already a revocable edge, so a soft-deleted one would be a second way of saying what revoking says.

| Method | Request | Response | Description |
| --- | --- | --- | --- |
| `GetMembership` | `GetMembershipRequest` | `Membership` | Retrieves one membership.    GET /v1/{name=ldapGroups/*/memberships/*}  Safe and idempotent. Returns the resource as stored; no field is computed per-caller. Read the `etag` here and pass it back on update or delete to make the write conditional.    NOT_FOUND          no such membership   INVALID_ARGUMENT   name does not match ldapGroups/*/memberships/*   PERMISSION_DENIED  caller may not read it |
| `ListMemberships` | `ListMembershipsRequest` | `ListMembershipsResponse` | Lists memberships.    GET /v1/{parent=ldapGroups/*}/memberships  Paginated per AIP-158: pass `page_size` (defaults to 50, capped at 1000) and the `next_page_token` from the previous response. An empty `next_page_token` means the last page. Page tokens are opaque and may expire; do not construct or store them.  Filtering uses the AIP-160 expression language over the resource's own fields. Results carry no guaranteed order.  Scoped to one group through `parent`; there is no cross-group listing. Filtering on `role` is the common case.    INVALID_ARGUMENT   malformed filter, or page_size out of range   PERMISSION_DENIED  caller may not list this collection |
| `CreateMembership` | `CreateMembershipRequest` | `Membership` | Creates a membership.    POST /v1/{parent=ldapGroups/*}/memberships   body: the membership itself  Server-assigned fields on the request body are ignored: `name`, `uid`, `etag` and the timestamps are set by the server regardless of what is sent. Supply `membership_id` to choose the final path segment; leave it empty and the server generates one.  Not idempotent. Retrying after a timeout may create a second membership; supply `membership_id` if the caller needs retry safety.    ALREADY_EXISTS     that membership_id is taken   INVALID_ARGUMENT   a field violates its buf.validate constraint   PERMISSION_DENIED  caller may not create here |
| `UpdateMembership` | `UpdateMembershipRequest` | `Membership` | Updates a membership.    PATCH /v1/{membership.name=ldapGroups/*/memberships/*}   body: the membership itself  A partial update: only fields named in `update_mask` are written. An empty mask writes every mutable field, which will clear anything the caller left unset -- pass a mask unless that is the intent. OUTPUT_ONLY and IMMUTABLE fields are rejected rather than silently dropped.  Idempotent. Pass the `etag` read from Get to make the write conditional.    NOT_FOUND          no such membership   ABORTED            etag does not match the stored resource   INVALID_ARGUMENT   mask names an unknown or immutable field   PERMISSION_DENIED  caller may not modify it |

## Messages

### Membership

Membership is one entry's presence in a Group.  This is the 'member' attribute of section 2.17 <https://www.rfc-editor.org/rfc/rfc4519.html#section-2.17> promoted to a resource. It exists because a membership carries attributes of its own -- at minimum a role -- and an attribute needs something to hang on.  Section 3.6 offers 'groupOfUniqueNames' with 'uniqueMember' (section 2.40 <https://www.rfc-editor.org/rfc/rfc4519.html#section-2.40>), which appends an optional UID to the DN so a reused name does not silently inherit an old membership. The uid field below solves that problem more directly, so only 'member' is modelled.  Reference: RFC 4519 "LDAP: Schema for User Applications", section 2.17. https://www.rfc-editor.org/rfc/rfc4519.html#section-2.17

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `IDENTIFIER` | Resource name, "ldapGroups/{ldap_group}/memberships/{membership}". |
| `uid` | `string` | `OUTPUT_ONLY` | Server-assigned unique id. |
| `member` | `string` | `REQUIRED` | 'member', section 2.17 <https://www.rfc-editor.org/rfc/rfc4519.html#section-2.17>. Any resource: section 3.5 <https://www.rfc-editor.org/rfc/rfc4519.html#section-3.5> types member as a distinguished name, which in a directory may address a person, a group or anything else with an entry. |
| `role` | `string` | `OPTIONAL` | What the member does in the group, e.g. "owner", "approver".  Native: RFC 4519 has no concept of a role within a group. It is here because every real use of a group -- approvals, ownership, escalation -- needs to distinguish members, and the RFC's answer is a separate group per role, which multiplies groups instead of describing them. |
| `create_time` | `Timestamp` | `OUTPUT_ONLY` | When the membership was created. |
| `update_time` | `Timestamp` | `OUTPUT_ONLY` | When the membership was last modified. |
| `etag` | `string` | `OUTPUT_ONLY` | Weak validator for optimistic concurrency, AIP-154 <https://aip.dev/154>. Pass it back in an update or delete to make the write conditional: if the resource changed since it was read, the request fails instead of overwriting. |

### GetMembershipRequest

Request message for Memberships.GetMembership, AIP-131 <https://aip.dev/131>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the membership, "ldapGroups/{ldap_group}/memberships/{membership}". |

### ListMembershipsRequest

Request message for Memberships.ListMemberships, AIP-132 <https://aip.dev/132>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `parent` | `string` | `REQUIRED` | Group whose memberships to list, "ldapGroups/{ldap_group}". |
| `page_size` | `int32` | `OPTIONAL` | Maximum memberships to return. Defaults to 50, capped at 1000. |
| `page_token` | `string` | `OPTIONAL` | Page token from a previous ListMembershipsResponse.next_page_token. |
| `filter` | `string` | `OPTIONAL` | Filter expression, AIP-160 <https://aip.dev/160>. Filtering on role is the common case. |

### ListMembershipsResponse

Response message for Memberships.ListMemberships, AIP-132 <https://aip.dev/132>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `memberships` | `repeated Membership` | - | The memberships on this page. |
| `next_page_token` | `string` | - | Token for the next page, empty when this is the last page. |

### CreateMembershipRequest

Request message for Memberships.CreateMembership, AIP-133 <https://aip.dev/133>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `parent` | `string` | `REQUIRED` | Group to add the member to, "ldapGroups/{ldap_group}". |
| `membership` | `Membership` | `REQUIRED` | The membership to create. |
| `membership_id` | `string` | `OPTIONAL` | Client-assigned id, becoming the last segment of the resource name. The server generates one when empty. |

### UpdateMembershipRequest

Request message for Memberships.UpdateMembership, AIP-134 <https://aip.dev/134>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `membership` | `Membership` | `REQUIRED` | The membership to update. Its name field identifies the resource. In practice only role is mutable: changing member would make this a different membership. |
| `update_mask` | `FieldMask` | `OPTIONAL` | Fields to update. An empty mask updates all mutable fields. |

### DeleteMembershipRequest

Request message for Memberships.DeleteMembership, AIP-135 <https://aip.dev/135>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the membership to revoke. |

---

© 2026 The Protobuf Project Authors | Apache 2.0 License
