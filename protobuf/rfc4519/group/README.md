# Group Service

**Package:** `protobuf.rfc4519.group.v1`

> [!IMPORTANT]
> Auto-generated by `protodoc`. Do not edit manually.

## Services

### LdapGroups

LdapGroups manages the RFC 4519 'groupOfNames' object class, section 3.5 <https://www.rfc-editor.org/rfc/rfc4519.html#section-3.5>.  Membership is a separate service in its own package. Splitting them puts a service boundary between a group and its members, so creating a group with members is two calls and is not atomic. That is the cost of one package per resource; callers that need both must tolerate a partially created group.

| Method | Request | Response | Description |
| --- | --- | --- | --- |
| `GetLdapGroup` | `GetLdapGroupRequest` | `LdapGroup` | Retrieves one group.    GET /v1/{name=ldapGroups/*}  Safe and idempotent. Returns the resource as stored; no field is computed per-caller. Read the `etag` here and pass it back on update or delete to make the write conditional.    NOT_FOUND          no such group, or it is soft-deleted and `show_deleted` was not set   INVALID_ARGUMENT   name does not match ldapGroups/*   PERMISSION_DENIED  caller may not read it |
| `ListLdapGroups` | `ListLdapGroupsRequest` | `ListLdapGroupsResponse` | Lists groups.    GET /v1/ldapGroups  Paginated per AIP-158: pass `page_size` (defaults to 50, capped at 1000) and the `next_page_token` from the previous response. An empty `next_page_token` means the last page. Page tokens are opaque and may expire; do not construct or store them.  Filtering uses the AIP-160 expression language over the resource's own fields. Results carry no guaranteed order.    INVALID_ARGUMENT   malformed filter, or page_size out of range   PERMISSION_DENIED  caller may not list this collection |
| `CreateLdapGroup` | `CreateLdapGroupRequest` | `LdapGroup` | Creates a group.    POST /v1/ldapGroups   body: the group itself  Server-assigned fields on the request body are ignored: `name`, `uid`, `etag` and the timestamps are set by the server regardless of what is sent. Supply `ldap_group_id` to choose the final path segment; leave it empty and the server generates one.  Not idempotent. Retrying after a timeout may create a second group; supply `ldap_group_id` if the caller needs retry safety.    ALREADY_EXISTS     that ldap_group_id is taken   INVALID_ARGUMENT   a field violates its buf.validate constraint   PERMISSION_DENIED  caller may not create here |
| `UpdateLdapGroup` | `UpdateLdapGroupRequest` | `LdapGroup` | Updates a group.    PATCH /v1/{ldap_group.name=ldapGroups/*}   body: the group itself  A partial update: only fields named in `update_mask` are written. An empty mask writes every mutable field, which will clear anything the caller left unset -- pass a mask unless that is the intent. OUTPUT_ONLY and IMMUTABLE fields are rejected rather than silently dropped.  Idempotent. Pass the `etag` read from Get to make the write conditional.    NOT_FOUND          no such group   ABORTED            etag does not match the stored resource   INVALID_ARGUMENT   mask names an unknown or immutable field   PERMISSION_DENIED  caller may not modify it |
| `UndeleteLdapGroup` | `UndeleteLdapGroupRequest` | `LdapGroup` | Restores a soft-deleted group.    POST /v1/{name=ldapGroups/*}:undelete  Valid only before `expire_time`; after that the group is gone and this returns NOT_FOUND. Clears `delete_time` and `expire_time` and returns the group as it was.    NOT_FOUND          no such group, or already purged   ALREADY_EXISTS     the group is not deleted   PERMISSION_DENIED  caller may not restore it |

## Messages

### LdapGroup

LdapGroup is the LDAP 'groupOfNames' object class, RFC 4519 section 3.5 <https://www.rfc-editor.org/rfc/rfc4519.html#section-3.5>.  Section 3.5 makes 'member' a mandatory multi-valued attribute of the group itself. This model deliberately does not: membership is Membership, a resource of its own. A repeated string of DNs cannot carry a role, cannot be created or revoked independently, and cannot be listed with a page token. Externalising it is the difference between a group that records who is in it and one that can answer why.  A consequence: 'member' is mandatory in the RFC and absent here, so a LdapGroup with no Membership is representable in this API and is not a valid groupOfNames entry.  The rule for an exporter: refuse, not synthesise. Section 3.5 gives no value that could stand in for an absent member -- inventing one (the group's own DN, an owner, a sentinel) asserts a membership nothing in this model states, which misleads every LDAP consumer of the export more than an explicit error would. An exporter targeting LDAP MUST reject a LdapGroup with zero Memberships rather than emit an invalid or fabricated entry.  Reference: RFC 4519 "LDAP: Schema for User Applications", section 3.5. https://www.rfc-editor.org/rfc/rfc4519.html#section-3.5

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `IDENTIFIER` | Resource name, "ldapGroups/{ldap_group}". |
| `uid` | `string` | `OUTPUT_ONLY` | Server-assigned unique id. |
| `display_names` | `repeated string` | `REQUIRED` | 'cn', section 2.3 <https://www.rfc-editor.org/rfc/rfc4519.html#section-2.3>. Mandatory per section 3.5 <https://www.rfc-editor.org/rfc/rfc4519.html#section-3.5>. |
| `descriptions` | `repeated string` | `OPTIONAL` | 'description', section 2.5 <https://www.rfc-editor.org/rfc/rfc4519.html#section-2.5>. |
| `owners` | `repeated string` | `OPTIONAL` | 'owner', section 2.21 <https://www.rfc-editor.org/rfc/rfc4519.html#section-2.21>. Distinguished names, opaque here. |
| `organization` | `string` | `OPTIONAL` | 'o', section 2.19 <https://www.rfc-editor.org/rfc/rfc4519.html#section-2.19>. Optional per section 3.5 <https://www.rfc-editor.org/rfc/rfc4519.html#section-3.5>: a groupOfNames need not belong to an organization. |
| `business_categories` | `repeated string` | `OPTIONAL` | 'businessCategory', section 2.1 <https://www.rfc-editor.org/rfc/rfc4519.html#section-2.1>. |
| `create_time` | `Timestamp` | `OUTPUT_ONLY` | When the group was created. |
| `update_time` | `Timestamp` | `OUTPUT_ONLY` | When the group was last modified. |
| `etag` | `string` | `OUTPUT_ONLY` | Weak validator for optimistic concurrency, AIP-154 <https://aip.dev/154>. Pass it back in an update or delete to make the write conditional: if the resource changed since it was read, the request fails instead of overwriting. |
| `delete_time` | `Timestamp` | `OUTPUT_ONLY` | When the group was soft-deleted, unset while it is live. AIP-164 <https://aip.dev/164>. |
| `expire_time` | `Timestamp` | `OUTPUT_ONLY` | When a soft-deleted group is purged for good. AIP-164 <https://aip.dev/164>. |

### GetLdapGroupRequest

Request message for LdapGroups.GetLdapGroup, AIP-131 <https://aip.dev/131>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the group to retrieve, "ldapGroups/{ldap_group}". |

### ListLdapGroupsRequest

Request message for LdapGroups.ListLdapGroups, AIP-132 <https://aip.dev/132>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `page_size` | `int32` | `OPTIONAL` | Maximum groups to return. Defaults to 50, capped at 1000. |
| `page_token` | `string` | `OPTIONAL` | Page token from a previous ListLdapGroupsResponse.next_page_token. |
| `filter` | `string` | `OPTIONAL` | Filter expression, AIP-160 <https://aip.dev/160>. |
| `show_deleted` | `bool` | `OPTIONAL` | Include soft-deleted groups in the result. AIP-164 <https://aip.dev/164>. |

### ListLdapGroupsResponse

Response message for LdapGroups.ListLdapGroups, AIP-132 <https://aip.dev/132>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `ldap_groups` | `repeated LdapGroup` | - | The groups on this page. |
| `next_page_token` | `string` | - | Token for the next page, empty when this is the last page. |

### CreateLdapGroupRequest

Request message for LdapGroups.CreateLdapGroup, AIP-133 <https://aip.dev/133>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `ldap_group` | `LdapGroup` | `REQUIRED` | The group to create. |
| `ldap_group_id` | `string` | `OPTIONAL` | Client-assigned id, becoming the last segment of the resource name. The server generates one when empty. |

### UpdateLdapGroupRequest

Request message for LdapGroups.UpdateLdapGroup, AIP-134 <https://aip.dev/134>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `ldap_group` | `LdapGroup` | `REQUIRED` | The group to update. Its name field identifies the resource. |
| `update_mask` | `FieldMask` | `OPTIONAL` | Fields to update. An empty mask updates all mutable fields. |

### DeleteLdapGroupRequest

Request message for LdapGroups.DeleteLdapGroup, AIP-135 <https://aip.dev/135>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the group to delete, "ldapGroups/{ldap_group}". |
| `force` | `bool` | `OPTIONAL` | Delete the group's memberships along with it.  Required by AIP-135 <https://aip.dev/135> because LdapGroup has child resources. Without it the request fails on a non-empty group, which is the safer default: a group is usually deleted because it is obsolete, and silently revoking every membership is not something to do by omission. |

### UndeleteLdapGroupRequest

Request message for UndeleteLdapGroup, AIP-164 <https://aip.dev/164>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the soft-deleted group to restore, "ldapGroups/{ldap_group}". |

---

© 2026 The Protobuf Project Authors | Apache 2.0 License
