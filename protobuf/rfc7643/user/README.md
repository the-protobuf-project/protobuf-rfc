# User Service

**Package:** `protobuf.rfc7643.user.v1`

> [!IMPORTANT]
> Auto-generated by `protodoc`. Do not edit manually.

## Services

### Users

Users manages identities in the canonical SCIM model.  **This is where identity data lives** under rule 14. The RFC 4519 packages keep their own CRUD and are not deprecated, but an LDAP entry saved through them is stored as a User here.  Unlike the contact lineage, that conversion follows no specification. RFC 9555 maps vCard to JSContact element by element; **nothing equivalent exists for SCIM and LDAP**, so the mapping is this project's own invention and is labelled as such wherever it appears. A consumer relying on it is relying on a local decision, not on a standard.  SCIM defines its own REST protocol in RFC 7644, which this service does not implement: these are the AIP-131..135 standard methods, and a SCIM front end maps onto them. The two disagree about pagination in particular -- see ListUsersRequest.page_token.

| Method | Request | Response | Description |
| --- | --- | --- | --- |
| `GetUser` | `GetUserRequest` | `User` | Retrieves one user.    GET /v1/{name=users/*}  Safe and idempotent. `password` is never returned, per RFC 7643 section 7.5 <https://www.rfc-editor.org/rfc/rfc7643.html#section-7.5>.    NOT_FOUND          no such user, or it is soft-deleted and `show_deleted` was not set   INVALID_ARGUMENT   name does not match users/*   PERMISSION_DENIED  caller may not read it |
| `ListUsers` | `ListUsersRequest` | `ListUsersResponse` | Lists users.    GET /v1/users  Paginated per AIP-158. Page tokens are opaque and may expire; do not construct or store them.    INVALID_ARGUMENT   malformed filter, or page_size out of range   PERMISSION_DENIED  caller may not list this collection |
| `CreateUser` | `CreateUserRequest` | `User` | Creates a user.    POST /v1/users   body: the user itself  Server-assigned fields on the request body are ignored: `name`, `uid`, `etag` and the timestamps are set by the server regardless. `groups` is OUTPUT_ONLY and is ignored too -- membership is changed through the Group resource, per RFC 7643 section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>.  Not idempotent. Retrying after a timeout may create a second user; supply `user_id` if the caller needs retry safety.    ALREADY_EXISTS     that user_id is taken, or `username` is not unique   INVALID_ARGUMENT   a field violates its buf.validate constraint   PERMISSION_DENIED  caller may not create here |
| `UpdateUser` | `UpdateUserRequest` | `User` | Updates a user.    PATCH /v1/{user.name=users/*}   body: the user itself  A partial update: only fields named in `update_mask` are written. An empty mask writes every mutable field, which will clear anything the caller left unset -- pass a mask unless that is the intent.  Deactivating a user is `active = false`, not a delete: RFC 7643 section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1> keeps a deactivated account addressable, which is the whole point of the attribute.  Idempotent. Pass the `etag` read from Get to make the write conditional.    NOT_FOUND          no such user   ABORTED            etag does not match the stored resource   INVALID_ARGUMENT   mask names an unknown or immutable field   PERMISSION_DENIED  caller may not modify it |
| `UndeleteUser` | `UndeleteUserRequest` | `User` | Restores a soft-deleted user.    POST /v1/{name=users/*}:undelete  Valid only before `expire_time`; after that the user is gone and this returns NOT_FOUND.    NOT_FOUND          no such user, or already purged   ALREADY_EXISTS     the user is not deleted   PERMISSION_DENIED  caller may not restore it |

## Messages

### EnterpriseUser

EnterpriseUser is the Enterprise User schema extension, RFC 7643 section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>.  Identified in SCIM by the URN "urn:ietf:params:scim:schemas:extension:enterprise:2.0:User". A schema extension in SCIM is a namespaced block of extra attributes on a resource, which is a nested message here -- it has no identity of its own and no lifecycle apart from the User, so rule 8 makes it a value object.  Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 4.3. https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `employee_number` | `string` | `OPTIONAL` | SCIM's `employeeNumber`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. The organization's own identifier for this person. |
| `cost_center` | `string` | `OPTIONAL` | SCIM's `costCenter`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. |
| `organization` | `string` | `OPTIONAL` | SCIM's `organization`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>.  A string, not a reference to protobuf.rfc4519.organization.v1's Organization: AIP-215 <https://aip.dev/215> permits no cross-package message reference, and section 4.3 types it as free text rather than as a resource anyway. |
| `division` | `string` | `OPTIONAL` | SCIM's `division`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. |
| `department` | `string` | `OPTIONAL` | SCIM's `department`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. |
| `manager` | `Manager` | `OPTIONAL` | SCIM's `manager`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. |

### Manager

Manager is the Enterprise User "manager" attribute, RFC 7643 section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>.  Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 4.3. https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `value` | `string` | `OPTIONAL` | The manager's SCIM `id`, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>, which is the `uid` of another User in this collection.  Not a google.api.resource_reference: that annotation names a resource *name* ("users/{user}") and section 4.3 types this as the bare id. The reference is real but the shape is the RFC's, not AIP's. |
| `reference_uri` | `string` | `OPTIONAL` | A URI resolving to the manager's User resource, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. SCIM spells this `$ref`, which is not a legal protobuf field name. |
| `display_name` | `string` | `OUTPUT_ONLY` | The manager's display name, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>. Section 4.3 makes it read-only: it is a denormalised copy of the manager's own attribute, and writing it here would let the two disagree. |

### GetUserRequest

Request message for Users.GetUser, AIP-131 <https://aip.dev/131>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the user to retrieve, "users/{user}". |

### ListUsersRequest

Request message for Users.ListUsers, AIP-132 <https://aip.dev/132>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `page_size` | `int32` | `OPTIONAL` | Maximum users to return. Defaults to 50, capped at 1000. |
| `page_token` | `string` | `OPTIONAL` | Page token from a previous ListUsersResponse.next_page_token.  AIP-158's opaque token, which is also how RFC 9865's cursor pagination is served: that document adds `cursor` and `nextCursor` to SCIM, and the two are the same mechanism under different names. Modelling both would give a caller two ways to page and no rule for choosing, so the AIP spelling is the one exposed and a SCIM front end maps onto it. |
| `filter` | `string` | `OPTIONAL` | Filter expression, AIP-160 <https://aip.dev/160>. |
| `show_deleted` | `bool` | `OPTIONAL` | Include soft-deleted users in the result. AIP-164 <https://aip.dev/164>. |

### ListUsersResponse

Response message for Users.ListUsers, AIP-132 <https://aip.dev/132>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `users` | `repeated User` | - | The users on this page. |
| `next_page_token` | `string` | - | Token for the next page, empty when this is the last page. |

### CreateUserRequest

Request message for Users.CreateUser, AIP-133 <https://aip.dev/133>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `user` | `User` | `REQUIRED` | The user to create. |
| `user_id` | `string` | `OPTIONAL` | Client-assigned id, becoming the last segment of the resource name. The server generates one when empty. |

### UpdateUserRequest

Request message for Users.UpdateUser, AIP-134 <https://aip.dev/134>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `user` | `User` | `REQUIRED` | The user to update. Its name field identifies the resource. |
| `update_mask` | `FieldMask` | `OPTIONAL` | Fields to update. An empty mask updates all mutable fields. |

### DeleteUserRequest

Request message for Users.DeleteUser, AIP-135 <https://aip.dev/135>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the user to delete, "users/{user}". |

### UndeleteUserRequest

Request message for Users.UndeleteUser, AIP-164 <https://aip.dev/164>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the soft-deleted user to restore, "users/{user}". |

### Attribute

Attribute is SCIM's default multi-valued attribute, RFC 7643 section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>.  One shape for emails, phone numbers, IMs, photos, entitlements, roles and certificates alike -- section 2.4 defines `value`, `display`, `type` and `primary` once and every multi-valued attribute reuses them. That uniformity is deliberate in SCIM and is why this is a single message rather than one per attribute, which is the opposite of the choice RFC 9553 forced in the JSContact package.  Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 2.4. https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `value` | `string` | `OPTIONAL` | The significant value, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>: the email address, the phone number, the certificate. |
| `display` | `string` | `OPTIONAL` | A human-readable label, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>. Section 2.4 gives it a mutability of "immutable"; that is a server-side rule this schema states rather than enforces. |
| `attribute_type` | `string` | `OPTIONAL` | What the value is for, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>, e.g. "work", "home", "mobile".  Free text rather than an enum: section 2.4 defines the sub-attribute once, but each attribute that uses it declares its own canonical values -- "work"/"home"/"other" for emails, "work"/"home"/"mobile"/"fax"/"pager" for phone numbers -- and section 7 lets a provider add more. One enum could not be right for all of them, and one per attribute would defeat the point of section 2.4.  Named `attribute_type`, not `type`: `type` is too close to the resource type in google.api.resource for a reader skimming both. |
| `primary` | `bool` | `OPTIONAL` | Whether this is the preferred value, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>. At most one entry in a given attribute may set it; that is a cross-field rule and is not expressed as a buf.validate constraint, because no gate here checks CEL -- see docs/conventions.md rule 12. |
| `reference_uri` | `string` | `OPTIONAL` | A URI referencing the resource this value points at, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>. SCIM spells this `$ref`, which is not a legal protobuf field name. |

### Name

Name is the User "name" attribute, RFC 7643 section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>.  Positional in the same way vCard's N is, and with the same limitation: five fixed slots plus a formatted string. SCIM predates RFC 9553's componentised model and has no equivalent, so a name that does not fit these slots loses structure here in a way it would not in a JSContact Card.  Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 4.1.1. https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `display_name` | `string` | `OPTIONAL` | The full name assembled for display, section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>, including all prefixes and suffixes.  SCIM calls this `formatted`; renamed because a bare adjective names no thing, and `display_name` is on AIP-122 <https://aip.dev/122>'s short allowlist for the `_name` suffix. |
| `family_name` | `string` | `OPTIONAL` | Family name, section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>. SCIM: `familyName`. |
| `given_name` | `string` | `OPTIONAL` | Given name, section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>. SCIM: `givenName`. |
| `middle_names` | `string` | `OPTIONAL` | Middle name, section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>. SCIM: `middleName`; renamed for AIP-122 <https://aip.dev/122>'s `_name` suffix rule, which permits only display_name, given_name and family_name. |
| `honorific_prefix` | `string` | `OPTIONAL` | Honorific prefix, e.g. "Ms", section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>. |
| `honorific_suffix` | `string` | `OPTIONAL` | Honorific suffix, e.g. "III", section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>. |

### Address

Address is the User "addresses" attribute, RFC 7643 section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>.  Section 4.1.2 gives addresses their own sub-attributes rather than reusing Attribute's `value`, which is why this is a message of its own.  Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 4.1.2. https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `full_address` | `string` | `OPTIONAL` | The full address as it should be written, newlines included, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. SCIM calls this `formatted`. |
| `street_address` | `string` | `OPTIONAL` | Street address, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. SCIM: `streetAddress`. |
| `locality` | `string` | `OPTIONAL` | City or locality, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. |
| `region` | `string` | `OPTIONAL` | State or region, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. |
| `postal_code` | `string` | `OPTIONAL` | Postal or ZIP code, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. SCIM: `postalCode`. |
| `region_code` | `string` | `OPTIONAL` | ISO 3166-1 alpha-2 code, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. SCIM calls this `country`; AIP-143 <https://aip.dev/143> requires `region_code` for an ISO 3166-1 value. |
| `attribute_type` | `string` | `OPTIONAL` | What the address is for, e.g. "work" or "home", section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. |
| `primary` | `bool` | `OPTIONAL` | Whether this is the preferred address, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>. |

### User

User is the SCIM User resource, RFC 7643 section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>.  **The canonical identity model** under rule 14, holding the plain name that RFC 6350's vCard gave up when it became `Vcard`. protobuf.rfc4519.group.v1 and its LDAP siblings remain first-class packages with their own CRUD; this is what identity data is stored as.  One caution that does not apply to the contact lineage: **there is no IETF-specified SCIM-to-LDAP conversion.** RFC 9555 maps vCard to JSContact element by element, so that codec follows a specification. Nothing equivalent exists here, so any mapping between this and RFC 4519 is a local decision and is labelled as one wherever it appears -- see rule 14 in docs/ontology.md.  SCIM's own `id` (section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>) is the service provider's identifier, and this schema *is* the service provider, so it maps to `uid` rather than to a second field. `external_uid` is the client's identifier, which is the one that arrives from outside and must survive a round trip -- the same split rule 9 draws everywhere else.  Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 4.1. https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `IDENTIFIER` | Resource name, "users/{user}". Assigned by the server. |
| `uid` | `string` | `OUTPUT_ONLY` | Server-assigned unique id, SCIM's `id`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>. |
| `external_uid` | `string` | `OPTIONAL` | SCIM's `externalId`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>. The identifier the provisioning client uses for this user in its own system.  Immutable and preserved verbatim: a provisioning client matches on it, so rewriting it would make every subsequent sync create a duplicate. |
| `username` | `string` | `REQUIRED` | SCIM's `userName`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. The login identifier, unique across the provider. REQUIRED by section 4.1.  Named `username` rather than `user_name`: AIP-122 <https://aip.dev/122> bans the `_name` suffix outside a three-value allowlist this is not on. |
| `name_components` | `Name` | `OPTIONAL` | SCIM's `name`, section 4.1.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.1>. Renamed because the resource name owns `name`, as it does in every package here. |
| `display_name` | `string` | `OPTIONAL` | SCIM's `displayName`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. |
| `nickname` | `string` | `OPTIONAL` | SCIM's `nickName`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. |
| `profile_uri` | `string` | `OPTIONAL` | SCIM's `profileUrl`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. A page describing the user. |
| `title` | `string` | `OPTIONAL` | SCIM's `title`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>, e.g. "Vice President". |
| `user_type` | `string` | `OPTIONAL` | SCIM's `userType`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>, e.g. "Employee", "Contractor". Free text: section 4.1 gives canonical values but permits others. |
| `preferred_language_code` | `string` | `OPTIONAL` | SCIM's `preferredLanguage`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. A BCP 47 tag, RFC 5646 <https://www.rfc-editor.org/rfc/rfc5646.html>. Named per AIP-143 <https://aip.dev/143>. |
| `locale` | `string` | `OPTIONAL` | SCIM's `locale`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. How to format dates and numbers for this user -- distinct from `preferred_language_code`, which is what language to address them in. |
| `time_zone` | `string` | `OPTIONAL` | SCIM's `timezone`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. An IANA zone name, e.g. "Europe/London". |
| `active` | `bool` | `OPTIONAL` | SCIM's `active`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>. Whether the account is in use.  Deliberately not named `state` or `status`: AIP-216 <https://aip.dev/216> reserves both for a server-owned lifecycle, and this is administrative intent rather than lifecycle. It is also distinct from `delete_time` below -- a deactivated user still exists, which is the whole point of the attribute. |
| `password` | `string` | `OPTIONAL` | SCIM's `password`, section 4.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1>.  INPUT_ONLY: section 7.5 requires a service provider never to return it, and the field behaviour says so in the schema rather than leaving it to an implementer to remember. |
| `emails` | `repeated Attribute` | `OPTIONAL` | SCIM's `emails`, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. |
| `phone_numbers` | `repeated Attribute` | `OPTIONAL` | SCIM's `phoneNumbers`, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. |
| `instant_messages` | `repeated Attribute` | `OPTIONAL` | SCIM's `ims`, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. Instant messaging addresses. |
| `photos` | `repeated Attribute` | `OPTIONAL` | SCIM's `photos`, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. |
| `addresses` | `repeated Address` | `OPTIONAL` | SCIM's `addresses`, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. |
| `groups` | `repeated Attribute` | `OUTPUT_ONLY` | SCIM's `groups`, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. The groups this user belongs to.  OUTPUT_ONLY: section 4.1.2 makes it read-only and says membership is changed through the Group resource, not here. That is the same one-way edge protobuf.rfc4519.membership.v1 already models for LDAP. |
| `entitlements` | `repeated Attribute` | `OPTIONAL` | SCIM's `entitlements`, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. Things the user is entitled to that are not group memberships. |
| `roles` | `repeated Attribute` | `OPTIONAL` | SCIM's `roles`, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. |
| `x509_certificates` | `repeated Attribute` | `OPTIONAL` | SCIM's `x509Certificates`, section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2>. DER-encoded certificates, base64 in the JSON form. |
| `enterprise` | `EnterpriseUser` | `OPTIONAL` | The Enterprise User extension, section 4.3 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.3>.  A field rather than a separate resource: section 4.3 defines it as a schema extension of User, sharing its identity and lifecycle, which is rule 8's test for a value object. |
| `etag` | `string` | `OUTPUT_ONLY` | Weak validator for optimistic concurrency, AIP-154 <https://aip.dev/154>. This is SCIM's `meta.version`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>. |
| `delete_time` | `Timestamp` | `OUTPUT_ONLY` | When this user was soft-deleted, unset while it is live. AIP-164 <https://aip.dev/164>. |
| `expire_time` | `Timestamp` | `OUTPUT_ONLY` | When a soft-deleted user is purged for good. AIP-164 <https://aip.dev/164>. |
| `create_time` | `Timestamp` | `OUTPUT_ONLY` | When this user was created. SCIM's `meta.created`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>. |
| `update_time` | `Timestamp` | `OUTPUT_ONLY` | When this user was last modified. SCIM's `meta.lastModified`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>. |

---

© 2026 The Protobuf Project Authors | Apache 2.0 License
