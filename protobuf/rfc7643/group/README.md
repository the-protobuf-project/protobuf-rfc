# Group Service

**Package:** `protobuf.rfc7643.group.v1`

> [!IMPORTANT]
> Auto-generated by `protodoc`. Do not edit manually.

## Services

### Groups

Groups manages groups in the canonical SCIM model.  Membership is changed here, by updating the group's `members`, and not through the User resource -- RFC 7643 section 4.1.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.1.2> makes `User.groups` read-only for exactly that reason. One direction owns the edge, which is the same discipline protobuf.rfc4519.membership.v1 keeps with a separate resource.  As with Users, there is **no IETF-specified conversion between SCIM and LDAP**; any mapping onto protobuf.rfc4519.group.v1 is this project's own.

| Method | Request | Response | Description |
| --- | --- | --- | --- |
| `GetGroup` | `GetGroupRequest` | `Group` | Retrieves one group.    GET /v1/{name=groups/*}  Safe and idempotent.    NOT_FOUND          no such group, or it is soft-deleted and `show_deleted` was not set   INVALID_ARGUMENT   name does not match groups/*   PERMISSION_DENIED  caller may not read it |
| `ListGroups` | `ListGroupsRequest` | `ListGroupsResponse` | Lists groups.    GET /v1/groups  Paginated per AIP-158. Page tokens are opaque and may expire.    INVALID_ARGUMENT   malformed filter, or page_size out of range   PERMISSION_DENIED  caller may not list this collection |
| `CreateGroup` | `CreateGroupRequest` | `Group` | Creates a group.    POST /v1/groups   body: the group itself  Members may be supplied at creation, unlike the LDAP lineage where a group and its memberships are two calls. Server-assigned fields on the request body are ignored.  Not idempotent. Retrying after a timeout may create a second group; supply `group_id` if the caller needs retry safety.    ALREADY_EXISTS     that group_id is taken   INVALID_ARGUMENT   a field violates its buf.validate constraint   PERMISSION_DENIED  caller may not create here |
| `UpdateGroup` | `UpdateGroupRequest` | `Group` | Updates a group.    PATCH /v1/{group.name=groups/*}   body: the group itself  A partial update: only fields named in `update_mask` are written. Naming `members` replaces the list wholesale; see UpdateGroupRequest.update_mask.  Idempotent. Pass the `etag` read from Get to make the write conditional.    NOT_FOUND          no such group   ABORTED            etag does not match the stored resource   INVALID_ARGUMENT   mask names an unknown or immutable field   PERMISSION_DENIED  caller may not modify it |
| `UndeleteGroup` | `UndeleteGroupRequest` | `Group` | Restores a soft-deleted group.    POST /v1/{name=groups/*}:undelete  Valid only before `expire_time`; after that the group is gone and this returns NOT_FOUND.    NOT_FOUND          no such group, or already purged   ALREADY_EXISTS     the group is not deleted   PERMISSION_DENIED  caller may not restore it |

## Messages

### Group

Group is the SCIM Group resource, RFC 7643 section 4.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.2>.  **The canonical group model** under rule 14, holding the plain name RFC 4519's `groupOfNames` gave up when it became `LdapGroup`.  Membership is a field here, not a separate resource. That is the opposite of the choice protobuf.rfc4519.membership.v1 made, and deliberately so: section 4.2 defines `members` as an attribute of the Group and RFC 7644 changes it by PATCHing the Group, so a separate Membership resource would invent an addressing scheme SCIM does not have. The LDAP package split it out because `member` there is a bare DN with no room for a role; SCIM's member has `type` and `$ref` and needs no such rescue.  The cost is the one rule 6 already names in reverse: creating a group with members is one call here and two in the LDAP package, and the two lineages therefore do not have matching call graphs.  Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 4.2. https://www.rfc-editor.org/rfc/rfc7643.html#section-4.2

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `IDENTIFIER` | Resource name, "groups/{group}". Assigned by the server. |
| `uid` | `string` | `OUTPUT_ONLY` | Server-assigned unique id, SCIM's `id`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>. |
| `external_uid` | `string` | `OPTIONAL` | SCIM's `externalId`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>. The provisioning client's own identifier, preserved verbatim so a sync matches rather than duplicates. |
| `display_name` | `string` | `REQUIRED` | SCIM's `displayName`, section 4.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.2>. REQUIRED by section 4.2. |
| `members` | `repeated Member` | `OPTIONAL` | SCIM's `members`, section 4.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.2>. |
| `etag` | `string` | `OUTPUT_ONLY` | Weak validator for optimistic concurrency, AIP-154 <https://aip.dev/154>. SCIM's `meta.version`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>. |
| `delete_time` | `Timestamp` | `OUTPUT_ONLY` | When this group was soft-deleted, unset while it is live. AIP-164 <https://aip.dev/164>. |
| `expire_time` | `Timestamp` | `OUTPUT_ONLY` | When a soft-deleted group is purged for good. AIP-164 <https://aip.dev/164>. |
| `create_time` | `Timestamp` | `OUTPUT_ONLY` | When this group was created. SCIM's `meta.created`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>. |
| `update_time` | `Timestamp` | `OUTPUT_ONLY` | When this group was last modified. SCIM's `meta.lastModified`, section 3.1 <https://www.rfc-editor.org/rfc/rfc7643.html#section-3.1>. |

### Member

Member is one entry of a Group's "members", RFC 7643 section 4.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.2>.  Section 4.2 makes every sub-attribute immutable: a membership is added and removed, never edited in place. That is the same reasoning that made protobuf.rfc4519.membership.v1's Membership delete permanently rather than soft-delete -- an edge either exists or it does not.  Reference: RFC 7643 "System for Cross-domain Identity Management: Core Schema", section 4.2. https://www.rfc-editor.org/rfc/rfc7643.html#section-4.2

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `value` | `string` | `REQUIRED` | The member's `uid`, section 4.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.2>. A User or a nested Group.  Not a google.api.resource_reference: section 4.2 types this as the bare id rather than as a resource name, and the User it refers to lives in another proto package, which AIP-215 <https://aip.dev/215> forbids naming from here anyway. |
| `reference_uri` | `string` | `OPTIONAL` | A URI resolving to the member, section 4.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.2>. SCIM spells this `$ref`, which is not a legal protobuf field name. |
| `attribute_type` | `string` | `OPTIONAL` | Whether the member is a "User" or a "Group", section 4.2 <https://www.rfc-editor.org/rfc/rfc7643.html#section-4.2>. A group may contain groups, which is how SCIM expresses nesting. |
| `display` | `string` | `OPTIONAL` | A human-readable label for the member, section 2.4 <https://www.rfc-editor.org/rfc/rfc7643.html#section-2.4>. |

### GetGroupRequest

Request message for Groups.GetGroup, AIP-131 <https://aip.dev/131>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the group to retrieve, "groups/{group}". |

### ListGroupsRequest

Request message for Groups.ListGroups, AIP-132 <https://aip.dev/132>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `page_size` | `int32` | `OPTIONAL` | Maximum groups to return. Defaults to 50, capped at 1000. |
| `page_token` | `string` | `OPTIONAL` | Page token from a previous ListGroupsResponse.next_page_token. See protobuf.rfc7643.user.v1.ListUsersRequest.page_token for why RFC 9865's cursor is served through this rather than alongside it. |
| `filter` | `string` | `OPTIONAL` | Filter expression, AIP-160 <https://aip.dev/160>. |
| `show_deleted` | `bool` | `OPTIONAL` | Include soft-deleted groups in the result. AIP-164 <https://aip.dev/164>. |

### ListGroupsResponse

Response message for Groups.ListGroups, AIP-132 <https://aip.dev/132>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `groups` | `repeated Group` | - | The groups on this page. |
| `next_page_token` | `string` | - | Token for the next page, empty when this is the last page. |

### CreateGroupRequest

Request message for Groups.CreateGroup, AIP-133 <https://aip.dev/133>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `group` | `Group` | `REQUIRED` | The group to create. |
| `group_id` | `string` | `OPTIONAL` | Client-assigned id, becoming the last segment of the resource name. The server generates one when empty. |

### UpdateGroupRequest

Request message for Groups.UpdateGroup, AIP-134 <https://aip.dev/134>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `group` | `Group` | `REQUIRED` | The group to update. Its name field identifies the resource. |
| `update_mask` | `FieldMask` | `OPTIONAL` | Fields to update. An empty mask updates all mutable fields.  Naming `members` replaces the membership list wholesale. RFC 7643 section 4.2 makes each member's sub-attributes immutable, so there is nothing to merge into -- a membership is added or removed, never edited. |

### DeleteGroupRequest

Request message for Groups.DeleteGroup, AIP-135 <https://aip.dev/135>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the group to delete, "groups/{group}". |

### UndeleteGroupRequest

Request message for Groups.UndeleteGroup, AIP-164 <https://aip.dev/164>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the soft-deleted group to restore, "groups/{group}". |

---

© 2026 The Protobuf Project Authors | Apache 2.0 License
