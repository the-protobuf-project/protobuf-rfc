# Project Service

**Package:** `protobuf.shared.project.v1`

> [!IMPORTANT]
> Auto-generated by `protodoc`. Do not edit manually.

## Services

### Projects

Projects manages Project resources.  Native: no RFC defines a project, and the reason this resource exists at all is argued in project.proto. The service itself is plain AIP-131..135 plus AIP-164 undelete -- there is nothing standards-derived about it, which is why it lives under shared/ and not in an rfc*/ directory where the path alone would lend it authority it has not earned.

| Method | Request | Response | Description |
| --- | --- | --- | --- |
| `GetProject` | `GetProjectRequest` | `Project` | Retrieves one project.    GET /v1/{name=projects/*}  Safe and idempotent. Returns the resource as stored; no field is computed per-caller. Read the `etag` here and pass it back on update or delete to make the write conditional.    NOT_FOUND          no such project, or it is soft-deleted and `show_deleted` was not set   INVALID_ARGUMENT   name does not match projects/*   PERMISSION_DENIED  caller may not read it |
| `ListProjects` | `ListProjectsRequest` | `ListProjectsResponse` | Lists projects.    GET /v1/projects  Paginated per AIP-158: pass `page_size` (defaults to 50, capped at 1000) and the `next_page_token` from the previous response. An empty `next_page_token` means the last page. Page tokens are opaque and may expire; do not construct or store them.  Filtering uses the AIP-160 expression language over the resource's own fields. Results carry no guaranteed order.    INVALID_ARGUMENT   malformed filter, or page_size out of range   PERMISSION_DENIED  caller may not list this collection |
| `CreateProject` | `CreateProjectRequest` | `Project` | Creates a project.    POST /v1/projects   body: the project itself  Server-assigned fields on the request body are ignored: `name`, `uid`, `etag` and the timestamps are set by the server regardless of what is sent. Supply `project_id` to choose the final path segment; leave it empty and the server generates one.  Not idempotent. Retrying after a timeout may create a second project; supply `project_id` if the caller needs retry safety.    ALREADY_EXISTS     that project_id is taken   INVALID_ARGUMENT   a field violates its buf.validate constraint   PERMISSION_DENIED  caller may not create here |
| `UpdateProject` | `UpdateProjectRequest` | `Project` | Updates a project.    PATCH /v1/{project.name=projects/*}   body: the project itself  A partial update: only fields named in `update_mask` are written. An empty mask writes every mutable field, which will clear anything the caller left unset -- pass a mask unless that is the intent. OUTPUT_ONLY and IMMUTABLE fields are rejected rather than silently dropped.  Idempotent. Pass the `etag` read from Get to make the write conditional.    NOT_FOUND          no such project   ABORTED            etag does not match the stored resource   INVALID_ARGUMENT   mask names an unknown or immutable field   PERMISSION_DENIED  caller may not modify it |
| `UndeleteProject` | `UndeleteProjectRequest` | `Project` | Restores a soft-deleted project.    POST /v1/{name=projects/*}:undelete  Valid only before `expire_time`; after that the project is gone and this returns NOT_FOUND. Clears `delete_time` and `expire_time` and returns the project as it was.    NOT_FOUND          no such project, or already purged   ALREADY_EXISTS     the project is not deleted   PERMISSION_DENIED  caller may not restore it |

## Messages

### GetProjectRequest

Request message for Projects.GetProject, AIP-131 <https://aip.dev/131>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the project to retrieve, "projects/{project}". |

### ListProjectsRequest

Request message for Projects.ListProjects, AIP-132 <https://aip.dev/132>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `page_size` | `int32` | `OPTIONAL` | Maximum projects to return. Defaults to 50, capped at 1000. |
| `page_token` | `string` | `OPTIONAL` | Page token from a previous ListProjectsResponse.next_page_token. |
| `filter` | `string` | `OPTIONAL` | Filter expression, AIP-160 <https://aip.dev/160>. |
| `show_deleted` | `bool` | `OPTIONAL` | Include soft-deleted projects in the result. AIP-164 <https://aip.dev/164>. |

### ListProjectsResponse

Response message for Projects.ListProjects, AIP-132 <https://aip.dev/132>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `projects` | `repeated Project` | - | The projects on this page. |
| `next_page_token` | `string` | - | Token for the next page, empty when this is the last page. |

### CreateProjectRequest

Request message for Projects.CreateProject, AIP-133 <https://aip.dev/133>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `project` | `Project` | `REQUIRED` | The project to create. |
| `project_id` | `string` | `OPTIONAL` | Client-assigned id, becoming the last segment of the resource name. The server generates one when empty. |

### UpdateProjectRequest

Request message for Projects.UpdateProject, AIP-134 <https://aip.dev/134>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `project` | `Project` | `REQUIRED` | The project to update. Its name field identifies the resource. |
| `update_mask` | `FieldMask` | `OPTIONAL` | Fields to update. An empty mask updates all mutable fields. |

### DeleteProjectRequest

Request message for Projects.DeleteProject, AIP-135 <https://aip.dev/135>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the project to delete, "projects/{project}". |

### UndeleteProjectRequest

Request message for Projects.UndeleteProject, AIP-164 <https://aip.dev/164>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the soft-deleted project to restore. |

### Project

Project is a scope of work with a lifecycle.  Native: no RFC defines it, and under the rules of this repository that demands a reason rather than a citation.  The reason is that it is not a Group. A Group (RFC 4519 section 3.5 <https://www.rfc-editor.org/rfc/rfc4519.html#section-3.5>) answers *who belongs*, and its membership edges already carry a role. A Project answers *what work is scoped and when* -- it starts, it ends, it is paused or abandoned, and none of that has anywhere to live on a groupOfNames.  The test applied before building it: if Project turned out to be a Group with a role, it would not exist, because Membership.role already covers that. It survives because of start_time, end_time and state, which are lifecycle rather than membership.  Membership of a project is deliberately absent. If a project needs members, they are a Group referenced from here, not a second membership mechanism -- two overlapping ways to say who belongs is worse than either alone.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `IDENTIFIER` | Resource name, "projects/{project}". |
| `uid` | `string` | `OUTPUT_ONLY` | Server-assigned unique id. |
| `display_name` | `string` | `REQUIRED` | Human-readable label. Required: a project with no name is unusable in any list, and there is no RFC here to say otherwise. |
| `description` | `string` | `OPTIONAL` | What the project is for. |
| `owner` | `string` | `OPTIONAL` | Who owns it.  Typed as any resource rather than a User, matching Membership.member. An owner may be a person or a group, and naming protobufrfc.dev/Vcard here would make a native package depend on RFC 6350 for its meaning without gaining anything. |
| `start_time` | `Timestamp` | `OPTIONAL` | When work is scheduled to begin. |
| `end_time` | `Timestamp` | `OPTIONAL` | When work is scheduled to end. |
| `state` | `ProjectState` | `OUTPUT_ONLY` | Where the project is in its lifecycle.  Named `state`, which AIP-216 permits only because it is OUTPUT_ONLY: the server derives it from the dates and from explicit transitions, and a client cannot set it directly. |
| `etag` | `string` | `OUTPUT_ONLY` | Weak validator for optimistic concurrency, AIP-154. Pass it back in an update or delete to make the write conditional. |
| `delete_time` | `Timestamp` | `OUTPUT_ONLY` | When the project was soft-deleted, unset while it is live. AIP-164. |
| `expire_time` | `Timestamp` | `OUTPUT_ONLY` | When a soft-deleted project is purged for good. AIP-164. |
| `create_time` | `Timestamp` | `OUTPUT_ONLY` | When the project was created. |
| `update_time` | `Timestamp` | `OUTPUT_ONLY` | When the project was last modified. |

## Enums

### ProjectState

ProjectState is where a project sits in its lifecycle.  Native, like Project itself. Named State rather than a synonym because AIP-216 <https://aip.dev/216> reserves the word for exactly this: a server-owned lifecycle enum on an OUTPUT_ONLY field. The iCalendar status enums could not use it because clients set those.

| Value | Number | Description |
| --- | --- | --- |
| `PROJECT_STATE_UNSPECIFIED` | 0 | Not specified. |
| `PROJECT_STATE_ACTIVE` | 1 | Work is under way. |
| `PROJECT_STATE_PAUSED` | 2 | Started and deliberately halted, expected to resume. |
| `PROJECT_STATE_COMPLETED` | 3 | Finished. |
| `PROJECT_STATE_CANCELLED` | 4 | Abandoned rather than finished. Distinct from COMPLETED because the two mean opposite things to anything reporting on delivery. |

---

© 2026 The Protobuf Project Authors | Apache 2.0 License
