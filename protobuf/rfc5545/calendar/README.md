# Calendar Service

**Package:** `protobuf.rfc5545.calendar.v1`

> [!IMPORTANT]
> Auto-generated by `protodoc`. Do not edit manually.

## Services

### Calendars

Calendars manages the collections that Events belong to, RFC 5545 section 3.4 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.4>.  Events are managed by protobuf.rfc5545.event.v1.Events, a separate service in its own package. Deleting a calendar therefore affects a resource this service cannot see, which is why DeleteCalendar requires force rather than cascading silently.  AIP-131..135 also happens to cover most of CalDAV, RFC 4791: MKCALENDAR (section 5.3.1 <https://www.rfc-editor.org/rfc/rfc4791.html#section-5.3.1>) is CreateCalendar; the calendar-query REPORT's time-range filter (section 7.8 <https://www.rfc-editor.org/rfc/rfc4791.html#section-7.8>) is ListEvents' AIP-160 filter over start/end; and If-Match concurrency (section 5.3.4 <https://www.rfc-editor.org/rfc/rfc4791.html#section-5.3.4>) is the `etag` every resource already carries, AIP-154 <https://aip.dev/154>. QueryFreeBusy is the one CalDAV capability with no standard-method equivalent.

| Method | Request | Response | Description |
| --- | --- | --- | --- |
| `GetCalendar` | `GetCalendarRequest` | `Calendar` | Retrieves one calendar.    GET /v1/{name=calendars/*}  Safe and idempotent. Returns the resource as stored; no field is computed per-caller. Read the `etag` here and pass it back on update or delete to make the write conditional.    NOT_FOUND          no such calendar, or it is soft-deleted and `show_deleted` was not set   INVALID_ARGUMENT   name does not match calendars/*   PERMISSION_DENIED  caller may not read it |
| `ListCalendars` | `ListCalendarsRequest` | `ListCalendarsResponse` | Lists calendars.    GET /v1/calendars  Paginated per AIP-158: pass `page_size` (defaults to 50, capped at 1000) and the `next_page_token` from the previous response. An empty `next_page_token` means the last page. Page tokens are opaque and may expire; do not construct or store them.  Filtering uses the AIP-160 expression language over the resource's own fields. Results carry no guaranteed order.    INVALID_ARGUMENT   malformed filter, or page_size out of range   PERMISSION_DENIED  caller may not list this collection |
| `CreateCalendar` | `CreateCalendarRequest` | `Calendar` | Creates a calendar.    POST /v1/calendars   body: the calendar itself  Server-assigned fields on the request body are ignored: `name`, `uid`, `etag` and the timestamps are set by the server regardless of what is sent. Supply `calendar_id` to choose the final path segment; leave it empty and the server generates one.  Not idempotent. Retrying after a timeout may create a second calendar; supply `calendar_id` if the caller needs retry safety.    ALREADY_EXISTS     that calendar_id is taken   INVALID_ARGUMENT   a field violates its buf.validate constraint   PERMISSION_DENIED  caller may not create here |
| `UpdateCalendar` | `UpdateCalendarRequest` | `Calendar` | Updates a calendar.    PATCH /v1/{calendar.name=calendars/*}   body: the calendar itself  A partial update: only fields named in `update_mask` are written. An empty mask writes every mutable field, which will clear anything the caller left unset -- pass a mask unless that is the intent. OUTPUT_ONLY and IMMUTABLE fields are rejected rather than silently dropped.  Idempotent. Pass the `etag` read from Get to make the write conditional.    NOT_FOUND          no such calendar   ABORTED            etag does not match the stored resource   INVALID_ARGUMENT   mask names an unknown or immutable field   PERMISSION_DENIED  caller may not modify it |
| `UndeleteCalendar` | `UndeleteCalendarRequest` | `Calendar` | Restores a soft-deleted calendar.    POST /v1/{name=calendars/*}:undelete  Valid only before `expire_time`; after that the calendar is gone and this returns NOT_FOUND. Clears `delete_time` and `expire_time` and returns the calendar as it was.    NOT_FOUND          no such calendar, or already purged   ALREADY_EXISTS     the calendar is not deleted   PERMISSION_DENIED  caller may not restore it |
| `QueryFreeBusy` | `QueryFreeBusyRequest` | `QueryFreeBusyResponse` | Reports busy periods for a calendar over a time window, without returning the events themselves, RFC 4791 section 7.10 <https://www.rfc-editor.org/rfc/rfc4791.html#section-7.10> (the free-busy-query REPORT).    POST /v1/{name=calendars/*}:query   body: the whole request message  A custom method, not List with a projection: RFC 4791 grants CALDAV:read-free-busy independently of read access to the events themselves, section 6.1.1 <https://www.rfc-editor.org/rfc/rfc4791.html#section-6.1.1> -- a caller who may see only availability must never be handed the underlying Event.    NOT_FOUND          no such calendar   INVALID_ARGUMENT   window.end_time is not after window.start_time   PERMISSION_DENIED  caller may not read free/busy for this calendar |

## Messages

### Calendar

Calendar is the iCalendar object, RFC 5545 section 3.4 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.4>.  The collection an Event belongs to. Event's resource name is "calendars/{calendar}/events/{event}", so this is the parent that name refers to; without it every event would live under a collection nothing could create or delete.  PRODID (section 3.7.3 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.7.3>) and VERSION (section 3.7.4 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.7.4>) are deliberately absent. Both describe the iCalendar *stream* rather than the calendar -- which product wrote the file, and which version of the format it used -- so they belong to the codec that produces the stream, not to a stored resource.  Reference: RFC 5545 "Internet Calendaring and Scheduling Core Object Specification (iCalendar)", section 3.4. https://www.rfc-editor.org/rfc/rfc5545.html#section-3.4

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `IDENTIFIER` | Resource name, "calendars/{calendar}". |
| `uid` | `string` | `OUTPUT_ONLY` | Server-assigned unique id. |
| `display_name` | `string` | `REQUIRED` | NAME, RFC 7986 section 5.1 <https://www.rfc-editor.org/rfc/rfc7986.html#section-5.1>. RFC 5545 gives the calendar object no human-readable name at all; 7986 added one because every client needed to label a calendar in a list.  REQUIRED here is ours, not the RFC's: 7986 makes NAME optional, but a calendar with no label is unusable in any list UI, so this API insists. |
| `description` | `string` | `OPTIONAL` | DESCRIPTION, RFC 7986 section 5.2 <https://www.rfc-editor.org/rfc/rfc7986.html#section-5.2>. |
| `scale` | `CalendarScale` | `OPTIONAL` | CALSCALE, section 3.7.1 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.7.1>. |
| `time_zone` | `TimeZone` | `OPTIONAL` | Default time zone for events that do not carry their own.  google.type.TimeZone rather than a bare string with a regex: it carries the IANA database version alongside the id, and the same zone id can mean different offsets across tzdata releases. Section 3.6.5 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.6.5> answers this with an embedded VTIMEZONE component, which restates a database IANA already publishes. |
| `etag` | `string` | `OUTPUT_ONLY` | Weak validator for optimistic concurrency, AIP-154 <https://aip.dev/154>. Pass it back in an update or delete to make the write conditional: if the resource changed since it was read, the request fails instead of overwriting. |
| `delete_time` | `Timestamp` | `OUTPUT_ONLY` | When the calendar was soft-deleted, unset while it is live. AIP-164 <https://aip.dev/164>. |
| `expire_time` | `Timestamp` | `OUTPUT_ONLY` | When a soft-deleted calendar is purged for good. AIP-164 <https://aip.dev/164>. |
| `create_time` | `Timestamp` | `OUTPUT_ONLY` | When the calendar was created. |
| `update_time` | `Timestamp` | `OUTPUT_ONLY` | When the calendar was last modified. |
| `extensions` | `repeated ExtensionProperty` | `OPTIONAL` | Properties this schema does not model, section 3.8.8.2 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.8.8.2>. X-WR-CALNAME and friends live here. |

### ExtensionProperty

CalendarScale is the CALSCALE property, RFC 5545 section 3.7.1 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.7.1>.  ExtensionProperty carries an iCalendar property this schema does not model.  Not a hole punched in a typed schema: RFC 5545 section 3.8.8.1 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.8.8.1> reserves IANA-registered properties and section 3.8.8.2 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.8.8.2> non-standard X- properties, and requires that applications ignore what they do not recognise rather than reject it. A calendar that round-trips without them is lossy by construction.  It is also what keeps the modelled properties honest. Anything the schema has not earned a typed field for survives here instead of forcing a speculative field table, and a property graduates out of this message when a consumer needs to query it.  Reference: RFC 5545 "Internet Calendaring and Scheduling Core Object Specification (iCalendar)", section 3.8.8.2. https://www.rfc-editor.org/rfc/rfc5545.html#section-3.8.8.2

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `key` | `string` | `REQUIRED` | The property name: an IANA-registered token or an X- name, e.g. "X-WR-CALNAME". Not called `name`, which would make api-linter treat this message as a resource. |
| `values` | `repeated string` | `OPTIONAL` | The property's values, one entry per comma-separated component. The schema cannot know the structure of a property it does not model, so these stay unparsed. |

### GetCalendarRequest

Request message for Calendars.GetCalendar, AIP-131 <https://aip.dev/131>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the calendar to retrieve, "calendars/{calendar}". |

### ListCalendarsRequest

Request message for Calendars.ListCalendars, AIP-132 <https://aip.dev/132>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `page_size` | `int32` | `OPTIONAL` | Maximum calendars to return. Defaults to 50, capped at 1000. |
| `page_token` | `string` | `OPTIONAL` | Page token from a previous ListCalendarsResponse.next_page_token. |
| `filter` | `string` | `OPTIONAL` | Filter expression, AIP-160 <https://aip.dev/160>. |
| `show_deleted` | `bool` | `OPTIONAL` | Include soft-deleted calendars in the result. AIP-164 <https://aip.dev/164>. |

### ListCalendarsResponse

Response message for Calendars.ListCalendars, AIP-132 <https://aip.dev/132>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `calendars` | `repeated Calendar` | - | The calendars on this page. |
| `next_page_token` | `string` | - | Token for the next page, empty when this is the last page. |

### CreateCalendarRequest

Request message for Calendars.CreateCalendar, AIP-133 <https://aip.dev/133>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `calendar` | `Calendar` | `REQUIRED` | The calendar to create. |
| `calendar_id` | `string` | `OPTIONAL` | Client-assigned id, becoming the last segment of the resource name. The server generates one when empty. |

### UpdateCalendarRequest

Request message for Calendars.UpdateCalendar, AIP-134 <https://aip.dev/134>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `calendar` | `Calendar` | `REQUIRED` | The calendar to update. Its name field identifies the resource. |
| `update_mask` | `FieldMask` | `OPTIONAL` | Fields to update. An empty mask updates all mutable fields. |

### DeleteCalendarRequest

Request message for Calendars.DeleteCalendar, AIP-135 <https://aip.dev/135>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the calendar to delete, "calendars/{calendar}". |
| `force` | `bool` | `OPTIONAL` | Delete the calendar's events along with it.  Required by AIP-135 <https://aip.dev/135> because Event is a child of Calendar. Without it the request fails on a non-empty calendar, which is the safer default: deleting a calendar silently destroying a year of events is not something to do by omission. |

### UndeleteCalendarRequest

Request message for Calendars.UndeleteCalendar, AIP-164 <https://aip.dev/164>.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | Name of the soft-deleted calendar to restore. |

### QueryFreeBusyRequest

Request message for Calendars.QueryFreeBusy, RFC 4791 section 7.10 <https://www.rfc-editor.org/rfc/rfc4791.html#section-7.10> (the free-busy-query REPORT).

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `name` | `string` | `REQUIRED` | The calendar to query, "calendars/{calendar}". |
| `window` | `Interval` | `REQUIRED` | The window to report busy periods within. Required, and both bounds with it, for the same reason as ExpandEventRequest.window: an open-ended query over every event ever scheduled has no sensible default. |

### QueryFreeBusyResponse

Response message for Calendars.QueryFreeBusy.

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `periods` | `repeated FreeBusyPeriod` | - | The busy periods found within the requested window. Free time is not listed; its absence from this result is what "free" means. |

### FreeBusyPeriod

FreeBusyPeriod is one interval from a free/busy query result, structurally what the FREEBUSY property, RFC 5545 section 3.8.4.3 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.8.4.3>, carries -- but computed on demand from stored events, not itself stored. VFREEBUSY (section 3.6.4 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.6.4>) has no existence independent of the events it summarises, so this schema gives it no resource, only this query result.  Reference: RFC 5545 "Internet Calendaring and Scheduling Core Object Specification (iCalendar)", section 3.8.4.3. https://www.rfc-editor.org/rfc/rfc5545.html#section-3.8.4.3

| Field | Type | Behavior | Description |
| --- | --- | --- | --- |
| `period` | `Interval` | - | The interval, half-open: busy_type applies from `start_time` up to but not including `end_time`. |
| `busy_type` | `BusyType` | - | Why the interval is reported busy. |

## Enums

### CalendarScale

Reference: RFC 5545 "Internet Calendaring and Scheduling Core Object Specification (iCalendar)", section 3.7.1. https://www.rfc-editor.org/rfc/rfc5545.html#section-3.7.1

| Value | Number | Description |
| --- | --- | --- |
| `CALENDAR_SCALE_UNSPECIFIED` | 0 | Not specified; treat as CALENDAR_SCALE_GREGORIAN, the default and the only scale section 3.7.1 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.7.1> defines. |
| `CALENDAR_SCALE_GREGORIAN` | 1 | GREGORIAN. |

### BusyType

BusyType is the FBTYPE parameter, RFC 5545 section 3.2.9 <https://www.rfc-editor.org/rfc/rfc5545.html#section-3.2.9>.  Reference: RFC 5545 "Internet Calendaring and Scheduling Core Object Specification (iCalendar)", section 3.2.9. https://www.rfc-editor.org/rfc/rfc5545.html#section-3.2.9

| Value | Number | Description |
| --- | --- | --- |
| `BUSY_TYPE_UNSPECIFIED` | 0 | Not specified. Absent from any FreeBusyPeriod this schema produces -- every period this API returns carries an explicit type. |
| `BUSY_TYPE_BUSY` | 1 | BUSY. The default FBTYPE when a source property does not state one. |
| `BUSY_TYPE_BUSY_UNAVAILABLE` | 2 | BUSY-UNAVAILABLE. Busy, and the interval cannot be scheduled at all. |
| `BUSY_TYPE_BUSY_TENTATIVE` | 3 | BUSY-TENTATIVE. Busy because of one or more tentatively scheduled events. |
| `BUSY_TYPE_FREE` | 4 | FREE. Included only if a server chooses to report explicit free gaps rather than relying on absence from the result to mean free. |

---

© 2026 The Protobuf Project Authors | Apache 2.0 License
